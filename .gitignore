.build-cache/
.uncoil-worktrees/
.claude/settings.local.json
# Signed, notarized build products. Never committed: they are large, they are
# rebuilt from a tag, and a repository is not a binary distribution channel.
exports/
.DS_Store
DerivedData/
*.xcodeproj

# ── The repository is the source code, not the workshop around it. ─────────
# Everything below exists on disk and keeps working locally, but a contributor
# gets the app's sources, the docs that describe it, and the repo's own files
# (README, LICENSE, NOTICE, CHANGELOG) — not this machine's operating manuals.
CLAUDE.md
AGENTS.md
TODO.md
.claude/
.uncoil/
scripts/
docs/history/
docs/roadmap/
